import React from 'react'
import { Button } from '@material-ui/core'
import { styled } from '@material-ui/core/styles'

export const MyButton = styled(Button)({ fontSize: 50, padding: 150, borderRadius: 30 });

// const
// return (
//   <Button variant="contained" color={color || 'primary'} size={'large'} style={{ fontSize: 50, padding: 150, borderRadius: 30 }}>
//     Hej
//   </Button>
// )
// }
